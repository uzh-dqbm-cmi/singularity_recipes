name: cfdna_tools Docker Image CI

on:
  push:
    branches: [ main ]
    paths: 
      - bioinformatics/cfdna_tools.dockerfile
      - bioinformatics/cfdna_tools_requirements.txt
  pull_request:
    branches: [ main ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: docker login
      env:
        DOCKER_USER: ${{secrets.DOCKER_USER}}
        DOCKER_PASSWORD: ${{secrets.DOCKER_PASSWORD}}
      run: |
        docker login -u $DOCKER_USER -p $DOCKER_PASSWORD  
    - name: Build the cfdna_tools's Docker image
      run: docker build --file bioinformatics/cfdna_tools.dockerfile -t ${{secrets.DOCKER_USER}}/cfdna_tools:$(date +'%Y-%m-%d') -t ${{secrets.DOCKER_USER}}/cfdna_tools:latest .
      # $(date +%s)
    - name: Docker Push
      run: docker push -a ${{secrets.DOCKER_USER}}/cfdna_tools
